/* Aula 11 - Vetores */

// Vetor é uma coisa q eu não tinha dado muita bola por não ter sido ensinado na faculdade mas ele foi ensinado nesse curso, é basicamente um tipo de lista só que um pouco mais fixo porém mais rápido

// Em Python, vetores meio que não existem, as pessoas associam eles à listas que não são a mesma coisa mas essencialmente vetores têm geralmente tamanho fixo, apenas um tipo de variável nos seus elementos, memória continua e são mais rápidos porém mais difíceis de serem editados.

// Para percorrer um vetor adicionando valores, é só usar um for onde o índice é usado também para o vetor:

/*
N = [0, 0, 0, 0, 0] // To usando Python só pra exemplificar
for i in range(N)
    N[i] = int(input("Digite um número: "))

Esse código percorre cada índice do vetor e adiciona um valor diferente
*/ 

// Para criar um vetor é só criar uma variável normalmente mas adicionar um [] no final do nome:

//int numeros[5] // Isso seria uma variáve int normal mas se tornou um vetor e o número dentro dele é a quantidade de valores
