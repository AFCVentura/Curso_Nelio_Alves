/* Aula 12 - Matrizes */

// Matrizes nada mais são do que vetores bi-dimensionais, em C este é o princípio, para criar uma matriz é basicamente igual ao vetor, mas declara-se o índice de linhas e depois de colunas:

// int numeros[linhas][colunas]

// Para percorrer a matriz, é só fazer um for dentro de outro, o de fora percorre as linhas e o de dentro percorre as colunas (pois, tendo em vista que você está dentro de uma linha da lista, percorrer as colunas nada mais é do que percorrer os elementos da linha)

// Preguiça de escrever o exemplo mas é bem fácil e a sintaxe nova é só pra declarar a matriz